VITE_API_BASE_URL=http://localhost:8000
VITE_AZURE_AD_CLIENT_ID=

# Local-only dev convenience: set to "dummy" or "dummy-admin" to view the app as that seeded
# account instead of signing in with Microsoft. Requires LOCAL_AUTH_BYPASS=true in backend/.env.
VITE_LOCAL_USER=
